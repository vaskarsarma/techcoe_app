<div class="col-lg-6 col-md-6">
  <div class="widget-wrapper wow fadeIn" data-wow-delay="0.4s">
    <div class="list-group">
      <h4 class="list-group-item active">Proffessional details</h4>
      <div class="list-group-item">
        <div class="profderrorPanel hidden">
        </div>
        <div class="alert alert-success hidden profdsuccessResult" role="alert">
          <strong>Well Done!</strong> You have successfully updated the proffessional details.
        </div>
        <div class="alert alert-danger hidden profderrorResult" role="alert">
          Currently there are some technical issue . Please try again later.
        </div>
        <form class="form-horizontal" id="frmprofldetails">
          <div class="form-group md-form">
            <label for="proffession">Proffession</label>
            <input type="text" class="form-control" id="proffession" name="proffession" autofocus="autofocus" value="{{proffessionaldetails.proffession}}">
          </div>

          <div class="form-group md-form">
            <label for="department">Department</label>
            <input type="text" class="form-control" id="department" name="department" value="{{proffessionaldetails.department}}">
          </div>

          <div class="form-group md-form">
            <label for="company">Company</label>
            <input type="text" class="form-control" id="company" name="company" value="{{proffessionaldetails.company}}">
          </div>

          <div class="form-group md-form">
            <label for="location">Experience</label>
            <input type="text" class="form-control" id="locations" name="locations" value="{{proffessionaldetails.locations}}">
          </div>

          <div class="form-group ">
            <button class="btn btn-info" type="submit">Save</button>
          </div>

          {{#if proffessionaldetails}}
          <input type="hidden" id='userid' name='userid' value="{{proffessionaldetails.userid}}">
          <input type="hidden" id='_id' name='_id' value="{{proffessionaldetails._id}}"> {{else}}
          <input type="hidden" id='userid' name='userid' value="{{user._id}}">
          <input type="hidden" id='_id' name='_id' value=""> {{/if}}

        </form>
      </div>
    </div>
  </div>
</div>