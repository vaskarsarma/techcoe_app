<div class="widget-wrapper wow fadeIn blogcategory" data-wow-delay="0.4s">
            <div class="list-group">
                <h4 class="list-group-item active">Blog Categories:</h4>
                {{!-- {{#if user}} --}}
                {{!-- {{#each category}} --}}
                {{!-- <a href="/home/{{this.key}}" data-key="{{this.key}}" class="list-group-item blogctid">{{this.name}}</a> --}}
                {{!-- {{/each}}  --}}
                {{!-- {{else}} --}}
                {{#each category}}
                <span data-key="{{this.key}}" class="list-group-item blogctid">{{this.name}}</span> 
                {{/each}}
                {{!-- {{/if}} --}}
            </div>
        </div>